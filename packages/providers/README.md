# Providers Package

This package will hold concrete provider implementations and adapter code.

It stays empty in Phase 1 so the runtime dependencies can be introduced deliberately in later phases.

