# Providers Namespace

This namespace will host provider-related modules contributed by the providers package.

The namespace is kept open so concrete integrations can be added without changing the core package layout.

