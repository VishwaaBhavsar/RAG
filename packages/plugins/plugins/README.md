# Plugin Root

This directory is the discovery root for concrete plugins used by the framework.

Phase 8 adds the first real plugin here: a PDF loader that registers itself through the core registry.
