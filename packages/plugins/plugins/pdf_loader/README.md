# PDF Loader Plugin

This plugin provides the framework's first concrete document loader.

It is intentionally self-contained: the loader implementation, manifest, config schema, and registration entry point all live in this directory.
