# Plugins Package

This package will hold the plugin manager, discovery, validation, and loading support.

The directory exists now to lock in the plugin boundary before runtime loading logic is implemented.

