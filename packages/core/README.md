# Core Package

This package will contain the framework's core engine and the abstractions that the rest of the system depends on.

It exists now as a documented shell to keep the future runtime code isolated from the application entry points.

