# Apps

This directory holds application entry points that consume the framework.

Phase 1 keeps these folders empty except for documentation so the backend architecture can settle before UI work begins.

